server:
  port: 9010
spring:
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://localhost:9010
      username: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
      enabled: true

